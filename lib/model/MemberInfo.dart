class MemberInfo {
  String? nickname;
  String? accessToken;
  String? refreshToken;
  bool? isRemembered;

  MemberInfo({
    this.nickname,
    this.accessToken,
    this.refreshToken,
    this.isRemembered
  });
}